#include "Stack.h"

#define MAX 10
int stack[MAX];
int top = 0; // use 0 ou -1. Decida conforme você achar melhor!

bool push(int value)
{
	// implemente a inserção.
	// se está cheia, retorne o valor FALSE
	// se conseguiu inserir, retorne o valor TRUE
	
}

bool pop(int* value)
{
	// implemente a remoção da pilha.
	// se está vazia, retorne o valor FALSE
	// se conseguiu inserir, retorne o valor TRUE
	// value deve receber o valor no topo da pilha (atenção passagem de parâmetro por referência)
	
}

bool empty()
{
	//verifique se está vazia
}

bool full()
{
	//verifique se está cheia
}

